var playerScore = 0;
var computerScore = 0;
var playButton = document.getElementById("play");
var resetButton = document.getElementById("reset");
var playerScoreDisplay = document.getElementById("playerScore");
var computerScoreDisplay = document.getElementById("computerScore");

function playPOG() {
	// Lógica para lanzar los POGs
}

function updateScore(result) {
	// Lógica para actualizar los puntajes
}

function resetGame() {
	// Lógica para reiniciar el juego
}

playButton.addEventListener("click", playPOG);
resetButton.addEventListener("click", resetGame);
